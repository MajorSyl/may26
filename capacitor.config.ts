import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'org.rcfsunset.app',
  appName: 'Rotary Club Freetown-Sunset',
  webDir: 'dist'
};

export default config;
